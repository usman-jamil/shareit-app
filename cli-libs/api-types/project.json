{
  "name": "share-api-types",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "targets": {
    "build": {
      "command": "dotnet build cli-libs/api-types/Share.Api.Types.csproj -c Release"
    },
    "generate-types": {
      "command": "refitter --settings-file .refitter --skip-validation"
    }
  }
}
