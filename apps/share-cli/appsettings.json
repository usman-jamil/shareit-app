{
  "//": "ShareApi settings live in the user's ~/.share/config.yaml (see `share config show`), not here.",
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  }
}
