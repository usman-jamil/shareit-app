{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "serve": {
      "command": "dotnet run --project apps/api --launch-profile http",
      "continuous": true
    },
    "build": {
      "command": "dotnet build apps/api/Api.csproj -c Release"
    }
  }
}
