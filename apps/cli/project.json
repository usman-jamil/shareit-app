{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "serve": {
      "command": "dotnet run --project apps/cli --launch-profile migrate",
      "continuous": true
    },
    "build": {
      "command": "dotnet build apps/cli/Cli.csproj -c Release"
    }
  }
}
