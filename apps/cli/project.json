{
  "name": "cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "migrate": {
      "command": "dotnet run --launch-profile migrate",
      "options": {
        "cwd": "apps/cli"
      }
    },
    "build": {
      "command": "dotnet build apps/cli/Cli.csproj -c Release"
    }
  }
}
